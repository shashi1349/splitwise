# Splitwise-Lite — LinkedIn announcement

## Five-line post (replace the bracketed URLs after deploy)

> Just shipped **Splitwise-Lite** — a full-stack group-expense splitter
> built end-to-end as a mono-repo (Java 21 + Spring Boot 3.3 backend,
> React 18 + TypeScript-strict frontend, Tailwind, React Query).
>
> The fun bit: a debt-simplification algorithm using two max-heaps that
> always settles every group in **≤ N − 1 transfers** in **O(N log N)** —
> with deterministic remainder allocation so two clients never disagree.
>
> 38 backend + 21 frontend unit tests, RFC 7807 errors, JWT auth, all
> money math in integer cents, multi-stage Dockerfiles, one-click
> Render + Vercel deploy.
>
> Code → github.com/shashi1349/splitwise
> API → [render-url-here]
> App → [vercel-url-here]
>
> Always happy to talk about the trade-offs — drop a comment.

## Suggested hashtags

`#SoftwareEngineering` `#SpringBoot` `#React` `#TypeScript` `#Algorithms`
`#FullStack` `#Java`
